Add full intake flow, extraction prompts, Backboard robustness, and frontend debug

Backend
------
- main: Full /assess flow with intake → extraction → triage; use intake_question
  and intake_question_prompt for follow-up questions; store/get last question.
- backboard_client: Robust send_message: raise on HTTP errors, try multiple
  response shapes (content, message.content, messages[]), log full response when
  content missing, 60s timeout; fallback message when assistant returns null.
- intake: Extended with store_last_question / get_last_question and richer
  intake state for multi-turn collection.
- extraction: Uses extraction_prompt.txt for structured field extraction from
  user replies.
- New prompts: extraction_prompt.txt, intake_question_prompt.txt.

Frontend
--------
- Fix response field: use assistant_question (not intake_question) so AI
  follow-up questions display in chat.
- Add debug panel: "Debug – last response JSON" shows full /assess response
  for debugging (pretty-printed, scrollable).

No secrets committed; .env remains gitignored.
